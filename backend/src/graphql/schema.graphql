type Query {
  tags: [String!]!
  songs(tag: String!): SongPage!
  song(id: ID!): Song
}

type Song {
  id: ID!
  author: String!
  title: String!
  textWithChords: String!
  tags: [String!]!
}

type SongPage {
  total: Int!
  list: [Song!]!
}
